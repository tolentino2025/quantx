# Copie para .env e preencha antes de rodar docker compose up

# Banco de dados
POSTGRES_PASSWORD=troque_esta_senha_forte
POSTGRES_USER=quantx
POSTGRES_DB=quantx

# Porta pública do backend (padrão 3000)
BACKEND_PORT=3000
